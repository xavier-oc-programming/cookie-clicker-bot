# Python 3.10+
selenium>=4.0.0

# Selenium 4.6+ includes Selenium Manager which automatically downloads the
# correct ChromeDriver for your installed Chrome version — no manual setup needed.
#
# Bot detection is handled via ChromeOptions flags:
#   --disable-blink-features=AutomationControlled
#   excludeSwitches: enable-automation
#   useAutomationExtension: False
# plus a CDP Page.addScriptToEvaluateOnNewDocument call that hides
# navigator.webdriver before the page script runs.
# undetected-chromedriver is NOT used: it produces patched binaries that macOS
# Gatekeeper kills on Apple Silicon (SIGKILL, exit code 137).
